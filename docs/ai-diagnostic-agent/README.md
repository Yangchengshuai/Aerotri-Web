# AI Diagnostic Agent

AeroTri-Web 集成的 AI 诊断 Agent，基于 OpenClaw 实现自动错误诊断和修复建议。

## 功能特性

- **自动诊断**: 任务失败时自动触发诊断流程
- **上下文收集**: 收集系统状态、日志、参数等信息
- **AI 分析**: 调用 OpenClaw 进行智能分析
- **知识积累**: 自动更新知识库 `AerotriWeb_AGENT.md`
- **非阻塞**: 诊断流程异步执行，不影响主任务流程
- **容错处理**: 诊断失败不影响主任务

---

## 架构设计

### 核心组件

#### 1. Diagnostic Context Collector
**文件**: `app/services/diagnostic_context_collector.py`

收集诊断所需的上下文信息：
- Block 信息（ID、名称、算法、参数）
- 系统状态（CPU、GPU、内存）
- 日志片段（最近 500 行）
- 错误阶段和消息

#### 2. OpenClaw Diagnostic Agent
**文件**: `app/services/openclaw_diagnostic_agent.py`

与 OpenClaw 交互：
- 调用 OpenClaw CLI
- 发送诊断请求
- 解析 JSON 响应
- 更新知识库

#### 3. Diagnostic Router
**文件**: `app/services/diagnostic_router.py`

管理诊断流程：
- 任务失败钩子集成
- 异步非阻塞执行
- 错误容错处理

---

## 集成点

诊断 Agent 已集成到以下 Runner:

| Runner | 触发条件 | 钩子位置 |
|--------|----------|----------|
| `task_runner.py` | SfM 任务失败 | 4 个异常处理点 |
| `openmvs_runner.py` | OpenMVS 重建失败 | 2 个异常处理点 |
| `gs_runner.py` | 3DGS 训练失败 | 1 个异常处理点 |
| `tiles_runner.py` | Tiles 转换失败 | 4 个异常处理点 |

---

## 配置

在 `aerotri-web/backend/config/observability.yaml` 中配置:

```yaml
diagnostic:
  enabled: true                    # 启用诊断 Agent
  openclaw_cmd: "openclaw"         # OpenClaw 命令
  agent_id: "main"                 # Agent ID
  timeout_seconds: 180             # 超时时间（3分钟）
```

---

## 使用指南

### 自动触发

诊断在任务失败时**自动触发**，无需手动操作。

**示例代码**:
```python
# task_runner.py 中的实际实现
try:
    await self._run_task_impl(block, gpu_index)
except Exception as e:
    # 自动触发诊断（非阻塞）
    try:
        from .diagnostic_router import on_task_failure
        asyncio.create_task(on_task_failure(
            block_id=block.id,
            task_type="sfm",
            error_message=str(e),
            stage=block.current_stage,
            auto_fix=True,
        ))
    except Exception as diag_e:
        # 诊断失败不应影响主流程
        logger.warning(f"[DIAGNOSTIC] Failed to trigger: {diag_e}")
    # 重新抛出原始异常
    raise
```

### 诊断流程

```
┌─────────────────────────────────────────────────────────┐
│                    任务失败                              │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│              触发诊断钩子                                │
│  • 检查 auto_diagnose 标志                              │
│  • 创建异步任务                                         │
│  • 非阻塞执行                                           │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│          收集诊断上下文                                  │
│  • Block 元数据                                         │
│  • 系统状态（CPU, GPU, 内存）                          │
│  • 最近 500 行日志                                      │
│  • 任务参数                                             │
│  • 错误阶段和消息                                       │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│          调用 OpenClaw 分析                             │
│  openclaw agent --agent main --message "<prompt>"      │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│          更新知识库                                     │
│  • AerotriWeb_AGENT.md（新模式）                       │
│  • diagnosis_history.log（所有记录）                   │
└─────────────────────────────────────────────────────────┘
```

---

## 诊断输出

### 知识库文件

#### 1. AerotriWeb_AGENT.md
存储新发现的错误模式和解决方案。

**格式**:
```markdown
## 错误模式: [错误名称]

**触发条件**:
- 阶段: xxx
- 症状: xxx

**根本原因**:
...

**解决方案**:
...

**预防措施**:
...

**相关代码**:
- file.py:line
```

#### 2. diagnosis_history.log
记录所有诊断历史。

**格式**:
```
[2026-02-16 10:30:00] Block: block-123 | Task: sfm | Stage: mapping
Error: Bundle adjustment failed to converge
Diagnosis: [OpenClaw response]
```

---

## 实际效果示例

### 场景：任务失败触发 AI 诊断

以下截图展示了完整的诊断流程，从任务失败到 AI 分析完成：

![诊断Agent实际效果](../pictures/诊断Agent.png)

**流程说明**：

1. **任务失败触发** (第一条消息)
   - SfM 任务在 `mapping` 阶段失败
   - 错误信息：`Camera parameter check failed`
   - 立即发送失败通知到 Block 运行通知群

2. **自动诊断触发**
   - 失败通知发送后，后台自动调用 OpenClaw Agent
   - 收集诊断上下文：
     - Block 信息（算法类型、参数）
     - 系统状态（GPU、内存）
     - 日志片段（最近 500 行）
     - 错误阶段和消息

3. **AI 分析完成** (第二条消息)
   - OpenClaw Agent 分析完成（耗时约 10-30 秒）
   - 发送诊断结果到同一群聊
   - 包含完整的错误分析：

**诊断结果内容**：
```
### 🤖 AI 诊断分析

**错误类型**: 相机模型不兼容

**根本原因**:
COLMAP 使用 OPENCV 相机模型进行重建，但后续 3DGS 训练仅支持
PINHOLE/SIMPLE_PINHOLE 模型。需要运行 undistortion 进行相机模型转换。

**相关代码**:
- aerotri-web/backend/app/services/gs_runner.py:145
  检查相机模型的逻辑

**修复建议**:
1. ✅ 自动运行 image_undistorter 进行模型转换
   ```bash
   colmap image_undistorter \
     --image_path images/ \
     --input_path sparse/0 \
     --output_path sparse_undistort/0 \
     --output_colmap_txt
   ```

2. 或在 SfM 参数中直接指定支持的模型：
   ```yaml
   feature_params:
     camera_model: "SIMPLE_PINHOLE"  # 直接使用 3DGS 兼容模型
   ```

3. 重新运行 3DGS 训练
```

4. **知识库自动更新**
   - 诊断结果保存到 `AerotriWeb_AGENT.md`
   - 历史记录保存到 `diagnosis_history.log`
   - 下次遇到相同错误可直接检索

**关键特性**：
- ✅ **完全自动化**：无需手动干预，失败即诊断
- ✅ **非阻塞**：诊断在后台异步执行，不影响主流程
- ✅ **上下文精准**：只收集相关信息，避免信息过载
- ✅ **知识积累**：每次诊断都丰富知识库

---

## 常见诊断场景

### 1. CUDA OOM (Out of Memory)
**症状**: `RuntimeError: CUDA out of memory`

**诊断要点**:
- GPU 内存使用情况
- batch_size 设置
- 图像分辨率

**自动修复建议**:
- 降低 batch_size
- 减小图像分辨率
- 减少迭代次数

### 2. Bundle Adjustment 失败
**症状**: `Bundle adjustment failed to converge`

**诊断要点**:
- 图像数量和重叠度
- min_num_matches 参数
- GPS prior 是否启用

**自动修复建议**:
- 提高图像重叠度（重新采集）
- 增加 min_num_matches
- 启用 GPS prior

### 3. 特征提取失败
**症状**: `Feature extraction failed`

**诊断要点**:
- 图像格式和大小
- EXIF 信息完整性
- GPU 可用性

**自动修复建议**:
- 检查图像格式
- 降低图像分辨率
- 切换到 CPU 模式

---

## 开发者指南

### 添加新的诊断钩子

在新的 Runner 中添加诊断支持：

```python
# 1. 导入诊断模块
from .diagnostic_router import on_task_failure

# 2. 在异常处理中触发
try:
    await run_your_task()
except Exception as e:
    # 触发诊断
    try:
        asyncio.create_task(on_task_failure(
            block_id=block.id,
            task_type="your_task_type",
            error_message=str(e),
            stage=current_stage,
            auto_fix=False,  # 根据需要设置
        ))
    except Exception as diag_e:
        logger.warning(f"[DIAGNOSTIC] Failed: {diag_e}")
    raise  # 重新抛出原始异常
```

### 自定义上下文收集器

扩展上下文收集器以支持新的任务类型：

```python
# diagnostic_context_collector.py

class DiagnosticContextCollector:
    async def collect_your_task_context(self, block_id: str) -> Dict:
        # 收集特定任务的上下文
        context = {
            "task_specific_field": get_value(),
            # ...
        }
        return context
```

---

## 测试与验证

### 手动测试

```bash
# 1. 确保配置启用
vim aerotri-web/backend/config/observability.yaml
# 设置 diagnostic.enabled = true

# 2. 触发一个会失败的任务
# 诊断将自动触发

# 3. 检查日志
tail -f data/outputs/diagnosis_history.log

# 4. 检查知识库更新
cat AerotriWeb_AGENT.md
```

### 单元测试

诊断 Agent 的单元测试暂未实现，但以下测试覆盖已在计划中：
- 上下文收集测试
- OpenClaw 调用测试
- 知识库更新测试
- 钩子集成测试

---

## 故障排查

### 诊断未触发

**检查清单**:
1. ✅ `observability.yaml` 中 `diagnostic.enabled = true`
2. ✅ OpenClaw CLI 已安装: `openclaw --version`
3. ✅ 任务确实抛出了异常
4. ✅ 查看日志中是否有 `[DIAGNOSTIC]` 前缀的消息

### OpenClaw 调用失败

**常见原因**:
- OpenClaw 未安装或不在 PATH 中
- agent_id 不存在
- 超时时间过短

**解决方法**:
```bash
# 检查 OpenClaw
openclaw --version

# 测试 agent
openclaw agent --agent main --message "test" --json
```

### 知识库未更新

**检查**:
- 文件权限
- 磁盘空间
- 日志中的写入错误

---

## 相关文档

- [AI 协作理念](../06-ai-collaboration/philosophy.md)
- [测试报告](../06-ai-collaboration/test-report.md)
- [通知配置](../NOTIFICATION_SETUP.md)
- [CLAUDE.md](../CLAUDE.md)

---

**更新时间**: 2026-02-16
**维护者**: Aerotri-Web Team
