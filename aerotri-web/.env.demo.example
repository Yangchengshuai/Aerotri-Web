# Optional demo runtime overrides for ./demo-up.sh

# Backend runtime
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
# Example custom start command:
# BACKEND_START_CMD=uvicorn app.main:app --host 127.0.0.1 --port 8000

# Cloudflare Pages target
CF_PAGES_PROJECT=aerotri-web-demo
CF_PAGES_BRANCH=demo

# If backend is already running elsewhere, set:
# DEMO_SKIP_BACKEND_START=1
